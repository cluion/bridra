// Package framework provides Bridra's typed application lifecycle with deterministic
// provider startup and shutdown, layered Config sources, Container bindings and
// request scopes, provider manifests, typed events, queued listeners, and background
// jobs, fixed-delay task scheduling, SQL database lifecycle and transaction boundaries,
// versioned schema migrations, grouped RPC routing, middleware, method policies, typed
// validation rules, exception rendering, and transport adapters.
package framework
