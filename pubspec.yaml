name: bridra
description: Bridra - a Go-powered application framework for Flutter, by Cluion.
publish_to: 'none'
version: 0.1.0
repository: https://github.com/cluion/bridra

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  bridra_flutter:
    path: packages/bridra_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
